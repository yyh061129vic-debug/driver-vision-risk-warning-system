# 数据索引目录

数据按“原始数据、可重建处理结果、可版本化索引”分层管理：

- `../data_raw/`：下载或采集的原始数据，只读保存。
- `../data_processed/`：转换、预处理和统一标注后的结果，必须能由脚本重建。
- `indexes/`：训练、验证、测试样本清单及其格式定义，可提交 Git。

训练集、验证集和测试集应按序列或场景划分，禁止相邻帧跨集合泄漏。真实数据本体不进入 Git。
